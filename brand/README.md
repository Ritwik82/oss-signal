# OSS Signal — FOSS Android app health dashboard

A measured, source-backed design system extracted from **https://oss-signal.vercel.app/**.

*Apps you rely on* — Prioritize actively maintained FOSS Android apps. Track the ones you rely on and discover the ones worth installing — every score is built from six transparent health signals.

## Product Overview / Context

OSS Signal is a health dashboard for open-source Android apps. It scores every project 1–10 from six transparent, weighted signals — Recency (0.24), Momentum (0.20), Abandonment Risk ↓ (0.20, inverted), Issue Health (0.16), Contributors (0.12), License (0.08) — and organizes apps into a Watchlist, Fresh Finds (last 9 months, no abandoned projects), a full Archive, and a Method explainer. Visually it is a **near-black glass UI**: serif Playfair Display headlines, Space Grotesk body, JetBrains Mono system labels, an accent teal oscilloscope waveform, a circuit-board hero, warm amber abandonment indicators, and corner-mark glass cards — with an operations-console voice (technical, mechanical, calm).

Source: [https://oss-signal.vercel.app/](https://oss-signal.vercel.app/) · Registered design system id: `user:oss-signal-vercel-app` · Brand extraction id: `oss-signal-f3cef3`

## Source evidence

Every token below was measured from the live site's CSS (`:root` custom properties, `@font-face` declarations, computed component styles), not guessed. Provenance and measurement notes live in `brand.json`, `BRAND.md`, and `context/source-context.md`. The site carries no raster imagery — it is typographic and icon-led; `imagery/oss-signal-full-site.png` is the preserved full-page capture.

## Package contents

- `brand.json` — machine-readable brand spec: 7 color roles (hex + OKLCH), typography (display/body/mono with Google Fonts URLs), voice, imagery, layout posture
- `brand.json.seed` — engine-level token overrides (controlHeight 40, 4px unit scale)
- `DESIGN.md` — canonical design rules (palette, type, voice, imagery, layout posture)
- `BRAND.md` — prose brand guide an autonomous design agent can follow
- `guide.md` — short generated brand reference
- `brand.html` — the live, editable brand-kit preview page (the extraction UI)
- `logos/` — preserved brand assets: `wordmark-lockup.svg` (primary), `wordmark-wave.svg`, `favicon-1.ico`, `header-inline.svg`
- `imagery/` — preserved captures: `oss-signal-full-site.png`
- `assets/` — uploaded source asset `oss-signal-full.png`
- `prefetch/` — raw source capture (page.html, styles.css) used for measurement
- `context/` — extraction provenance and review contract
- `system/` — generated design-system kit (regenerated by `od brand finalize`):
  - `kit.html`, `kit.dark.html`, `index.html` — component-kit pages
  - `variables.css`, `variables.dark.css`, `tokens.default.json`, `tokens.dark.json`, `tokens.compact.json`, `theme.json`, `seed.json`
  - `artifacts/landing.html`, `artifacts/deck.html`, `artifacts/poster.html`, `artifacts/email.html`, `artifacts/newsletter.html`, `artifacts/form.html` — six branded deliverables

## Preview Manifest

- `brand.html` — live brand kit (logo, palette, typography specimens, voice & tone, imagery & layout)
- `system/index.html` — design-system overview / launcher
- `system/kit.html` — component kit (light)
- `system/kit.dark.html` — component kit (dark)
- `system/artifacts/landing.html` — landing page
- `system/artifacts/deck.html` — 16:9 pitch deck
- `system/artifacts/poster.html` — print-style poster
- `system/artifacts/email.html` — bulletproof HTML email
- `system/artifacts/newsletter.html` — multi-story newsletter digest
- `system/artifacts/form.html` — brand-styled signup/contact form

## Reuse workflow

1. Bind tokens: read `DESIGN.md`, then `brand.json` for exact hex/OKLCH/font URLs.
2. Reference preserved assets from `logos/` and `imagery/` (relative paths; never hotlink).
3. Apply the dark-first glass language: 8px radius, 1px borders, white-alpha glass fills, 2px accent L-corners on cards, 8–10px uppercase JetBrains Mono micro-labels, one accent per viewport.
4. For new user-facing deliverables, regenerate or extend the six `system/artifacts/*` examples rather than rebuilding tokens.

## Regenerating

```bash
od brand preview oss-signal-f3cef3     # re-render brand.html from brand.json (live fill-in)
od brand finalize oss-signal-f3cef3    # re-validate + re-derive tokens and artifacts, update design system
```
